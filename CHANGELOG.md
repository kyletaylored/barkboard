# Changelog

## v1.1.0

Changes since v1.0.0:

- Update spacing (e8af7ce)
- Replace the collapse arrow with an explicit Open/Close pill, retitle (5abba4a)
- Make the Datadog section collapsible (closed by default), trim copy (9f43516)
- Fire a boot Event on every boot, not just abnormal ones (3dae3ce)
- Add API Calls by Endpoint widget for barkboard.api.calls (0307ff1)
- Update layout (93f7e0e)
- Fix widget name (b8f5cce)
- Update dashboard (ddf74e8)
- Have the device push its own metric metadata once per firmware version (5e08628)
- Push short_name/unit/description metadata for every barkboard.* metric (387c588)
- Extend memory checkpoints to after each list actually gets live data (3adc60e)
- Track API calls by endpoint as a new barkboard.api.calls metric (5c9d062)
- Fix on-call hero card: zero card padding so icon/name/email re-align (e356299)
- Add per-screen LVGL memory checkpoints to answer "where does it go" (5ccf302)
- Remove placeholder @pagerduty mention from device-offline monitor message (9ea9591)
- Add an Events widget + 5 monitors to the Datadog dashboard; publish them for download (485a708)
- add assets (ac631f6)
- Add a busy% proxy metric since real per-task CPU% isn't available (15865d2)
- Flasher: default to the latest release, rename main to Nightly, cap list to 3 (4ea4812)
- Fix pages.yml's manifest.json regression; make release.yml idempotent (2de528d)
- Drop the rc1 create/revert commits from v1.0.0's changelog (a7a078b)
- Release v1.0.0: publish versioned flasher artifacts + changelog (47fa3ad)
- Release v1.0.0: publish versioned flasher artifacts + changelog (65381c3)

## v1.0.0

First release.

- On-Call hero card: fix name/icon vertical misalignment, add email as secondary line (678b946)
- Cap incidents fetch at 5 items — confirmed live the buffer overflow is back (9346e5a)
- Fix og:url/image and JSON-LD to use the actual custom domain (d590918)
- Add author SEO metadata; stop duplicating the banner image (aaa4ccf)
- Flasher footer: real GitHub icon instead of a plain dot, drop Build Log link (461ee08)
- Sim: fix monitor/incident/on-call/SLO/bits data never rendering (f4298e6)
- Stop tracking sim/vendor/ (vendored LVGL + lv_drivers, ~99MB) (aad8cc2)
- Sim: fix the clock never showing up (1e17560)
- Stop tracking docs/'s stale "latest main" build output (74e3c77)
- Add opt-in auto-rotate and swipe-down-to-refresh (856334c)
- Remove the manual Team field; split device-metrics opt-in from Events (ea860e4)
- Add Open Graph/Twitter card image to the web flasher page (8b734e2)
- Portal: title the Device Info card, match its height to Datadog Keys, make Change Keys a real button (1b47cb3)
- Document the github-pages environment's tag-deployment restriction (4e34a02)
- Add release workflow with changelog + versioned flasher artifacts; public docs (1a8f414)
- Fix simulator build breaks from API drift; add make help (c55d316)
- Extend device self-monitoring; fix LVGL pool-exhaustion freeze; add Overview shortcut tiles (426441b)
- Add opt-in device self-monitoring: submit device-health metrics to Datadog (35b0852)
- Fix log/trace/RUM chart extraction for the newer formula()+variables[] monitor query style (ec85944)
- On-Call: only number escalation steps when there's more than one (be632b8)
- Confine scrolling on Bits Investigation Detail to its body, not the whole screen (1b51c1e)
- Restore server-side team scoping for Bits Investigations; single call, no fallback (8d7e247)
- Reduce Bits Investigations page_size 20->8: 20 overflows the buffered fetch (7ff8086)
- Redesign device settings portal to match the web flasher's visual language (7b8f9bb)
- Collapse Bits Investigations to one API call; filter by team client-side (fdac7df)
- Fix Bits Investigations: drop the ArduinoJson filter, it was silently matching zero items (5b9d8ac)
- Add temporary diagnostic to isolate the Bits Investigations empty-screen cause (29303c7)
- Restore team scoping for Bits Investigations, with a fallback to unscoped (e6fb1fd)
- Drop team scoping from Bits Investigations — it was silently hiding real results (a09521c)
- Add --raw to fetch_dd.py's investigations subcommand (9207b94)
- Add Bits investigation search/detail testing to tools/fetch_dd.py + Makefile (06137f7)
- Rewrite Bits Investigations on the real search API; add investigation detail screen (bd24637)
- Add chart support for trace-analytics and RUM alert monitors (0160949)
- Make active on-call prominent; add live status color to SLO rows (97860fb)
- Replace Monitor Detail's chart query/time text with x-axis "time ago" reference ticks (85895f7)
- Stamp a build-time version (git describe) into firmware and the web flasher's manifest (1fd6eff)
- Verify flash writes with MD5 readback; fix serial line-buffering (249bb49)
- Rework Monitor Detail's tap-to-inspect: vertical cursor line, per-point timestamp, drop the raw query row (1dae6f7)
- update button text (f622c7c)
- Fix reconnect race, persist active tab in URL, improve console UX (c6560f6)
- Split Connect into separate esptool and plain-serial modes (e4df69b)
- Add a Bits Investigations dashboard screen; make action-result feedback more visible (eaec4ec)
- Remove boards-soon badge; make Serial Terminal safe on real hardware (910a5b5)
- Widen Monitor Detail's marquee title box; add tap-to-inspect chart values (7a15121)
- update readme (73737cf)
- Test tip again (7d6dfd0)
- test tip box again (50f51b6)
- test tip box (f27a70f)
- Drop help card for a header Docs link, update footer credit (ca27a0a)
- Simplify web flasher page content and About modal copy (565ead3)
- Redesign web flasher UI and wire it to esptool-js directly (71a0325)
- Add log-alert monitor charting via the Logs Aggregate API (24aa878)
- Remove debugging checkpoint logging; add chart values and threshold reference lines to Monitor Detail (b42de4e)
- Fix the second-monitor freeze: stop calling lv_chart_remove_series()/add_series() on every view (5c96c72)
- Dump lv_mem_monitor() state right before the still-hanging lv_chart_remove_series() call (de6a578)
- Clamp chart lo/hi to lv_coord_t range and add checkpoints to bisect a second-monitor freeze (23d3229)
- Fix the Monitor Detail freeze: don't call lv_chart_remove_series() with a NULL series (2c5ccce)
- Add step-by-step checkpoints inside showMonitorDetail() to isolate the exact hanging line (cd1ac22)
- Update web flash (da7c543)
- simplify readme (43fe219)
- Add checkpoints around the netJob critical-section drain and MonitorDetail/PollAmbient dispatch to isolate the core-1 freeze (4a9d890)
- Add fine-grained loop() checkpoints to pinpoint the core-1 stall (45d5fe7)
- Add core-1/core-0 heartbeat logs to diagnose the Monitor Detail freeze (1ed982e)
- Add banner (d154b82)
- Add README screenshot, trim to essentials for a public audience (12de5b2)
- Correct README claims that drifted from this session's real fixes (a513c25)
- Initial commit: BarkBoard firmware, portal, tooling, and CI (4d64129)
